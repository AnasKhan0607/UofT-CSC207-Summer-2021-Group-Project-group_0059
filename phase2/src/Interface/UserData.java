package Interface;

/**
 * the interface for all navigator controllers
 */
public interface UserData {
    /**
     * the run method that is run during UserNavigators
     */
    public static void run(){};

    String currentUser();
}
